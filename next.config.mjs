/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["qx-plank.myshopify.com"],
  },
  /* config options here */
  reactCompiler: true,
};

export default nextConfig;
